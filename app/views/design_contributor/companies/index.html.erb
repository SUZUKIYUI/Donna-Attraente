<h1>企業一覧画面</h1>

<p>企業一覧</p>
<% @companies.each do |company| %>
  <%= link_to design_contributor_company_path(company.id) do %>
    <%= attachment_image_tag company, :profile_image, :fill, 400, 350, fallback: "no_image_company.png" %>
    <%= company.company_name %>
  <% end %>
<% end %>